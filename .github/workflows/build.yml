name: Build

on:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: LumeWeb/workflows/.github/workflows/build-plugin.yml@develop
    with:
      additional_dependencies: 'go.lumeweb.com/portal-plugin-core@develop,go.lumeweb.com/portal-plugin-quota@develop'
    secrets: inherit
